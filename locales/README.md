# Locales

Application translations live in `locales/app/`.

Use `app/en.json` as the reference file. Other language files should keep the same keys and translate values only.

Do not remove placeholders, formatting tokens, punctuation required by the UI, or structured values used by the app.